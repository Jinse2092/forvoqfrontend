# Copy this file to `.env` (do NOT commit `.env`) and fill values for your environment.
# Vite uses variables prefixed with VITE_ and exposes them to the client bundle.

## Frontend environment (client)
# Copy to `.env` for local development. Do NOT commit `.env` to source control.
# Backend URL the frontend will call for server-side exports/uploads
VITE_BACKEND_URL=https://forwokbackend-1.onrender.com

# WARNING: Do NOT store admin secrets in client-side env vars for production.
# For local testing only you may set `VITE_ADMIN_UPLOAD_SECRET` to match your backend's `ADMIN_UPLOAD_SECRET`.
# VITE_ADMIN_UPLOAD_SECRET=supersecret123
