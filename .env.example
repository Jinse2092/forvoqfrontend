# Copy this file to `.env` (do NOT commit `.env`) and fill values for your environment.
# Vite uses variables prefixed with VITE_ and exposes them to the client bundle.

# Google OAuth / Drive settings
# Replace the CLIENT_ID value with your OAuth client id (Web application)
VITE_GOOGLE_CLIENT_ID=56302885028-laan5qadd9mukfm8pbnj5f96dui58c3o.apps.googleusercontent.com
# Optional: API key (restrict referrers and Drive API)
VITE_GOOGLE_API_KEY=

# Note: Do NOT place or expose any CLIENT_SECRET in client-side code. If you need server-side uploads
# or app-owned Drive access, implement a backend endpoint and keep secrets server-side.
